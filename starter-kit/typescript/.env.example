# WebSocket address of the game server.
# Local:             ws://localhost:3000
# Cloudflare tunnel: wss://your-tunnel.trycloudflare.com
POKER_SERVER=ws://localhost:3000

# Must be unique per agent in the tournament
AGENT_ID=ts-agent-1

# Display name shown in the dashboard
AGENT_NAME=TypeScriptBot
