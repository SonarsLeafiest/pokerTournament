POKER_SERVER=ws://localhost:3000
AGENT_ID=cc-agent-1       # must be unique per instance
AGENT_NAME=ClaudeCodeBot
CLAUDE_MODEL=sonnet        # sonnet | opus | haiku  (no API key needed — uses Claude Code auth)
