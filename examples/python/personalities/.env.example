POKER_SERVER=ws://localhost:3000

# Override the agent ID and name if you're running multiple instances
# (each agent in a tournament must have a unique AGENT_ID)
AGENT_ID=ace-hunter
AGENT_NAME=AceHunter

# claude (default) | opus | haiku
CLAUDE_MODEL=sonnet
